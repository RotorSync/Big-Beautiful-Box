# Retired fleet maintenance-secret fingerprints — sha256 (hex, lowercase) of the
# secret bytes as persisted (bytes.strip()).
#
# A box whose persisted maintenance secret matches a line here is PURGED on its
# next in-app update, so it re-adopts the CURRENT shared fleet key on its next
# maintenance session (a dev-default box bootstrap-adopts the key the server
# carries in the signed OPEN frame). This is how a key is retired fleet-wide:
# add its fingerprint here. NEVER put the secret itself in this repo — it is
# public; fingerprints are irreversible.
#
# 2026-07-03: Bruce (.41) interim random key — boxes that adopted it during the
# ~6h window on 2026-07-02 before every company server moved to the shared
# Headings key (e.g. TR12). Retired in favor of the shared fleet key.
f025c2d50d8bac64cf3c2791ce7db46e2f1b1717b6368b5852fe2da618b00c6e
